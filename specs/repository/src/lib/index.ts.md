---
name: index.ts (lib)
description: 라이브러리 인덱스 파일
version: 1.0.0
type: typescript
category: library
tags: [export, index]
---

# index.ts (lib)

## 개요
`$lib` alias를 통해 import할 파일들을 배치하는 폴더입니다.

## 내용
```typescript
// place files you want to import through the `$lib` alias in this folder.
```
