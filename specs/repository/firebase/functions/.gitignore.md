---
title: "firebase/functions/.gitignore"
description: "Sonub 소스 코드 저장용 자동 생성 SED 스펙"
original_path: "firebase/functions/.gitignore"
spec_type: "repository-source"
---

# 목적
이 문서는 `firebase/functions/.gitignore` 파일의 전체 내용을 기록하여 SED 스펙만으로도 Sonub 프로젝트를 재구성할 수 있도록 합니다.

## 파일 정보
- 상대 경로: `firebase/functions/.gitignore`
- MIME: `text/plain`
- 유형: 텍스트

# 원본 소스 코드
```````text
# Compiled JavaScript files
lib/**/*.js
lib/**/*.js.map

# TypeScript v1 declaration files
typings/

# Node.js dependency directory
node_modules/
*.local
```````
