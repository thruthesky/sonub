---
name: components.json
description: components 컴포넌트
version: 1.0.0
type: configuration
category: component
original_path: components.json
---

# components.json

## 개요

**파일 경로**: `components.json`
**파일 타입**: configuration
**카테고리**: component

components 컴포넌트

## 소스 코드

```json
{
	"$schema": "https://shadcn-svelte.com/schema.json",
	"style": "default",
	"tailwind": {
		"config": "tailwind.config.js",
		"css": "src/app.css",
		"baseColor": "slate"
	},
	"aliases": {
		"components": "$lib/components",
		"utils": "$lib/utils"
	}
}

```

## 주요 기능

(이 섹션은 수동으로 업데이트 필요)

## 관련 파일

(이 섹션은 수동으로 업데이트 필요)
